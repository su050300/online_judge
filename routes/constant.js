//some constants that are used at many places in the project so this file is being imported wherever required

module.exports = {
    PORT : 3000,
    SESS_NAME:'user',
    SESS_SECRET:'secret word',
    SESS_LIFETIME:1000*60*60*2
}
